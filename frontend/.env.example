# Backend API URL — leave empty for local dev (Vite proxy handles it).
# In production, set to the full backend URL, e.g. https://council-api.onrender.com
VITE_API_URL=https://untitled-4jre.onrender.com
